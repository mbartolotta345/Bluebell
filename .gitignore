venv/
__pycache__/
*.pyc
.env
plants.db

# Raw design source files (logos, wallpapers, video, fonts) - already
# processed into optimized assets in static/
design-assets/

# Google OAuth client secret download - contains a real secret, never commit
client_secret_*.json
